How machine learning works: 
    basically we take the historical data, and label it(provide the output to that data), and then we train the machine learning model on that data. 
    and then we can use the model to predict the output for the new data.

NOTE: 
- add general notes to notes.txt
- add ml practical notes to ipynb file.

---------------------------------------------------------------------------------------------------------------------------------------
## Supervised learning: 
    It has two types: 
        - Regression: focuses on the numerical output/ values. 
            - Linear Regression:
            - Ridge
            - Lasso
        - Classification: focuses on the categorical output/ values. (yes/ no kind of op )
            for eg. whether a person is diabetic or not (yes/ no)
                - Trees
                - SVM 
                - KNN 
---------------------------------------------------------------------------------------------------------------------------------------
FOR CASE STUDIES: DO STUDY, PRACTICALS AND THEN NOTES: 

## CASE STUDY 1: ADVERTISING SALES PREDICTION : 
 // completed


## Scikit-learn video started: 
    // TOPIC: Classification
    // TOPIC STUDY: COMPLETED 
    // TOPIC PRACTICAL: Pending
    // TOPIC NOTES + DIVE DEEP + DOUBTS: Pending

        doubts: 
            what are scale sensitive algorithms 
            array([0]) in numpy means just a zero or it represents an element at 0th index..?
            one more time understand: scaled, fit_transform(X_train), and then transform(X_test)

























---------------------------------------------------------------------------------------------------------------------------------------
## Unsupervised learning:
    - K means
    - PCA 
    - Collaborative filtering

---------------------------------------------------------------------------------------------------------------------------------------
## General Notes: 
    Inputs: also called FEATURES/ INDEPENDENT VARIABLES. (represented by x)
    Outputs: also called LABELS/ DEPENDENT VARIABLES. (represented by y)